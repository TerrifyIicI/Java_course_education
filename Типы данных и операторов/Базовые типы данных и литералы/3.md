```
int a = 1;
{
    int b = 2;
}
int c = a + b;
```
Произойдет ошибка компиляции, так как время жизни объекта b ограничено фигурными скобками, что бы исправить это нам нужно вынести объект b за скобки или внести вычисление объекта с внутрь скобок, либо объявить объект b - глобальным.
