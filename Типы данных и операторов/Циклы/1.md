### Практика 1 — Бесконечные циклы while и do-while
`while`
```Java

while (true) {
    // код, который будет выполняться бесконечно
}
```
`do-while`
```Java

do {
    // код, который будет выполняться бесконечно
} while (true);
```
